import {
  Meta,
  Story,
  Description,
  Preview,
  Props,
} from '@storybook/addon-docs/blocks'

import Pagination from './Pagination'
import { withA11y } from '@storybook/addon-a11y'
import { withKnobs } from '@storybook/addon-knobs'
import Docs from '@venice/core/docs/Pagination.md'
import { knobPaginationIsMobile, knobPaginationIsLoading } from '@venice/core/storybook/knobs'


<Meta title="Components|Pagination" component={Pagination} />

<Description>{Docs}</Description>

### Default

<Preview>
  <Story name="Default" decorators={[withKnobs,withA11y]}>
    <div>
      <Pagination count={7} page={1} onChange={() => {}} isLoading={knobPaginationIsLoading()} />
      <Pagination count={7} page={4} onChange={() => {}} isLoading={knobPaginationIsLoading()} />
      <Pagination count={7} page={7} onChange={() => {}} isLoading={knobPaginationIsLoading()} />
    </div>
  </Story>
</Preview>

### Loading state

<Preview>
  <Story name="Loading state" decorators={[withKnobs,withA11y]}>
    <div>
      <Pagination count={10} page={7} onChange={() => {}} isLoading={true} />
    </div>
  </Story>
</Preview>

# Props

<Props of={Pagination} />
