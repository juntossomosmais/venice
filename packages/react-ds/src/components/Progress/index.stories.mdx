import { Meta, Story, Description, Preview } from '@storybook/addon-docs/blocks'
import { withKnobs } from '@storybook/addon-knobs'
import { withA11y } from '@storybook/addon-a11y'

import Docs from '@venice/core/docs/Progress.md'
import { knobProgressValue } from '@venice/core/storybook/knobs'

import { Progress } from '.'

<Meta title="Components|Progress" component={Progress} />

<Description>{Docs}</Description>

## Base

<Preview>
  <Story name="Base" decorators={[withKnobs, withA11y]}>
    <div>
      Without Label
      <Progress value={knobProgressValue()} style={{ width: '100%' }} /> <hr />
      With Label default
      <Progress value={knobProgressValue()} display /> <hr />
      With Label left
      <Progress value={knobProgressValue()} display="left" /> <hr />
      Without Label right
      <Progress value={knobProgressValue()} display="right" />
    </div>
  </Story>
  <Story name="Colors" decorators={[withKnobs, withA11y]}>
    <div>
      <Progress
        style={{ width: '100%', margin: '1em 0' }}
        color="primary"
        value={knobProgressValue()}
      />
      <Progress
        style={{ width: '100%', margin: '1em 0' }}
        color="secondary"
        value={knobProgressValue()}
      />
      <Progress
        style={{ width: '100%', margin: '1em 0' }}
        color="success"
        value={knobProgressValue()}
      />
      <Progress
        style={{ width: '100%', margin: '1em 0' }}
        color="danger"
        value={knobProgressValue()}
      />
      <br />
    </div>
  </Story>
</Preview>
