import { Meta, Story, Description, Preview } from '@storybook/addon-docs/blocks'
import { withKnobs } from '@storybook/addon-knobs'
import { withA11y } from '@storybook/addon-a11y'

import Docs from '@venice/core/docs/Alert.md'

import { Alert, TYPES, VALID_COLORS } from '.'
import { Alert as AlertIcon } from '../Icons'
import { Store } from '../Icons'
import { Star } from '../Icons'
import { Bell } from '../Icons'
import { Check } from '../Icons'


<Meta title="Components|Alert" component={Alert} />

<Description>{Docs}</Description>

## Base

<Preview>
  <Story name="Base">
    <div>
      <Alert>Default</Alert> <br/>
      <Alert closable>Closable</Alert> <br/>
      <Alert icon={false}>Whitout icon</Alert> <br/>
      <Alert closable icon={false}>
        Whitout icon and closable
      </Alert><br/>
      <Alert onClose={() => alert('Alert was Closed') }>
        Whitout <code>onClose</code> event
      </Alert>
    </div>
  </Story>
</Preview>

## Types

<Preview>
  <Story name="Types">
    <div>
      <Alert type="default">default</Alert><br/>
      <Alert type="success">success</Alert><br/>
      <Alert type="danger">danger</Alert><br/>
      <Alert type="toast">toast</Alert><br/>
      <Alert type="vip">vip</Alert>
    </div>
  </Story>
</Preview>

## Colors

<Preview>
  <Story name="Colors">
    <div>
      <Alert>default</Alert><br/>
      <Alert color="primary">primary</Alert><br/>
      <Alert color="secondary">secondary</Alert><br/>
      <Alert color="success">success</Alert><br/>
      <Alert color="danger">danger</Alert>
    </div>
  </Story>
</Preview>

## Icons

<Preview>
  <Story name="Icons">
    <div>
      <Alert icon={<Star/>}>Star</Alert><br/>
      <Alert color="secondary" icon={<Star />}>Star secondary</Alert><br/>
      <Alert type="toast" color="success" icon={<Star />}>Star toast secondary</Alert><br/>
      <Alert type="toast" icon={<Star />}>Star toast</Alert><br/>
      <Alert icon={<span>♫</span>}>Custom Font</Alert><br/>
      <Alert color="danger" icon={<span>☺</span>}>Custom Font</Alert><br/>
      <Alert color="secondary" icon={<span>♣</span>}>Custom Font</Alert>
    </div>
  </Story>
</Preview>
