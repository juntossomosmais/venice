import { Meta, Story, Description, Preview, Props } from '@storybook/addon-docs/blocks'
import { withKnobs } from '@storybook/addon-knobs'
import { withA11y } from '@storybook/addon-a11y'

import { knobButtonLoading, knobButtonSizes } from '@venice/core/storybook/knobs'
import Docs from '@venice/core/docs/Button.md'

import Button from './Button'

<Meta title="Components|Buttons" component={Button} />

<Description>{Docs}</Description>

## Variants

### Filled buttons

<Preview>
  <Story name="Filled" parameters={{ decorators: [withKnobs,withA11y] }}>
    <div class="row-items">
      <Button isLoading={knobButtonLoading()}>Default</Button>
      <Button isLoading={knobButtonLoading()} color="primary">Primary</Button>
      <Button isLoading={knobButtonLoading()} color="secondary">Secondary</Button>
      <Button isLoading={knobButtonLoading()} disabled>Disabled</Button>
    </div>
  </Story>
</Preview>

### Outlined buttons

<Preview>
  <Story name="Outlined" parameters={{ decorators: [withKnobs,withA11y] }}>
    <div class="row-items">
      <Button isLoading={knobButtonLoading()} variant="outlined">Default</Button>
      <Button isLoading={knobButtonLoading()} color="primary" variant="outlined">
        Primary
      </Button>
      <Button isLoading={knobButtonLoading()} color="secondary" variant="outlined">
        Secondary
      </Button>
      <Button isLoading={knobButtonLoading()} disabled variant="outlined">
        Disabled
      </Button>
    </div>
  </Story>
</Preview>

### Text buttons

<Preview>
  <Story name="Text" parameters={{ decorators: [withKnobs,withA11y] }}>
    <div class="row-items">
      <Button isLoading={knobButtonLoading()} variant="text">Default</Button>
      <Button isLoading={knobButtonLoading()} color="primary" variant="text">
        Primary
      </Button>
      <Button isLoading={knobButtonLoading()} color="secondary" variant="text">
        Secondary
      </Button>
      <Button isLoading={knobButtonLoading()} disabled variant="text">
        Disabled
      </Button>
    </div>
  </Story>
</Preview>

## Shapes

### Circle buttons

<Preview>
  <Story name="Circle" parameters={{ decorators: [withKnobs,withA11y] }}>
    <div>
      <div class="row-items">
        <Button isLoading={knobButtonLoading()} size={knobButtonSizes()} shape="circle">
          @
        </Button>
        <Button isLoading={knobButtonLoading()} color="primary" size={knobButtonSizes()} shape="circle">
          @
        </Button>
        <Button isLoading={knobButtonLoading()} color="secondary" size={knobButtonSizes()} shape="circle">
          @
        </Button>
        <Button isLoading={knobButtonLoading()} size={knobButtonSizes()} shape="circle" disabled>
          @
        </Button>
      </div>
      <div class="row-items">
        <Button isLoading={knobButtonLoading()} size={knobButtonSizes()} shape="circle" variant="outlined">@</Button>
        <Button isLoading={knobButtonLoading()} color="primary" variant="outlined" size={knobButtonSizes()} shape="circle">
          @
        </Button>
        <Button isLoading={knobButtonLoading()} color="secondary" variant="outlined" size={knobButtonSizes()} shape="circle">
          @
        </Button>
        <Button isLoading={knobButtonLoading()} size={knobButtonSizes()} shape="circle" variant="outlined" disabled>
          @
        </Button>
      </div>
      <div class="row-items">
        <Button isLoading={knobButtonLoading()} size={knobButtonSizes()} shape="circle" variant="text">@</Button>
        <Button isLoading={knobButtonLoading()} color="primary" variant="text" size={knobButtonSizes()} shape="circle">
          @
        </Button>
        <Button isLoading={knobButtonLoading()} color="secondary" variant="text" size={knobButtonSizes()} shape="circle">
          @
        </Button>
        <Button isLoading={knobButtonLoading()} size={knobButtonSizes()} shape="circle" variant="text" disabled>
          @
        </Button>
      </div>
    </div>
  </Story>
</Preview>

## Sizes

### Small buttons

<Preview>
  <Story name="Small" parameters={{ decorators: [withKnobs,withA11y] }}>
    <div>
      <div class="row-items">
        <Button isLoading={knobButtonLoading()} size="small">Default</Button>
        <Button isLoading={knobButtonLoading()} color="primary" size="small">
          Primary
        </Button>
        <Button isLoading={knobButtonLoading()} color="secondary" size="small">
          Secondary
        </Button>
        <Button isLoading={knobButtonLoading()} size="small" disabled>
          Disabled
        </Button>
      </div>
      <div class="row-items">
        <Button isLoading={knobButtonLoading()} size="small" variant="outlined">Default</Button>
        <Button isLoading={knobButtonLoading()} color="primary" variant="outlined" size="small">
          Primary
        </Button>
        <Button isLoading={knobButtonLoading()} color="secondary" variant="outlined" size="small">
          Secondary
        </Button>
        <Button isLoading={knobButtonLoading()} size="small" variant="outlined" disabled>
          Disabled
        </Button>
      </div>
      <div class="row-items">
        <Button isLoading={knobButtonLoading()} size="small" variant="text">Default</Button>
        <Button isLoading={knobButtonLoading()} color="primary" variant="text" size="small">
          Primary
        </Button>
        <Button isLoading={knobButtonLoading()} color="secondary" variant="text" size="small">
          Secondary
        </Button>
        <Button isLoading={knobButtonLoading()} size="small" variant="text" disabled>
          Disabled
        </Button>
      </div>
    </div>
  </Story>
</Preview>

## Icons

<Preview>
  <Story name="Icons" parameters={{ decorators: [withKnobs,withA11y] }}>
    <div class="row-items">
      <Button isLoading={knobButtonLoading()} startIcon="←" variant="outlined">With icon</Button>
      <Button isLoading={knobButtonLoading()} endIcon="→" variant="outlined">With icon</Button>
      <Button isLoading={knobButtonLoading()} startIcon={<span>←</span>} size="small">With icon and tag</Button>
    </div>
  </Story>
</Preview>

## Loading state

<Preview>
  <Story name="Loading state" parameters={{ decorators: [withKnobs,withA11y] }}>
    <div class="row-items">
      <Button isLoading={true} color="primary">Loading example</Button>
      <Button isLoading={true} color="secondary" size="small">Loading example</Button>
    </div>
  </Story>
</Preview>

# Props

<Props of={Button} />
