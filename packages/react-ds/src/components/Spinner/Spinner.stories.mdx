import { Meta, Story, Description, Preview, Props } from '@storybook/addon-docs/blocks'
import { withKnobs, number } from '@storybook/addon-knobs'
import { knobSpinnerSize } from '@venice/core/storybook/knobs'
import Docs from '@venice/core/docs/Spinner.md'

import Spinner from './Spinner'

<Meta title="Components|Spinners" component={Spinner} />

<Description>{Docs}</Description>

<Preview>
  <Story name="Indeterminated" parameters={{ decorators: [withKnobs] }}>
    <div class="row-items">
      <Spinner size={knobSpinnerSize()}></Spinner>
      <Spinner size={knobSpinnerSize()} color="primary"></Spinner>
      <Spinner size={knobSpinnerSize()} color="secondary"></Spinner>
    </div>
  </Story>
</Preview>

# Props

<Props of={Spinner} />
