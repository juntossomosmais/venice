import {  Description, Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks'
import { withA11y } from '@storybook/addon-a11y'
import { withKnobs } from '@storybook/addon-knobs'
import { knobPaginationIsLoading } from '@venice/core/storybook/knobs'

import Docs from '@venice/core/docs/Pagination.md'

import Pagination from './Pagination.vue'

<Meta title="Components|Pagination" component={Pagination} />

# Pagination

Pagination allow users to select a specific page from a page range.

## Default

<Preview>
  <Story name="Default" parameters={{ decorators: [withKnobs,withA11y] }} inline={true}>
    {{
      components: { Pagination },
      props: {
        isLoading: {
          default: knobPaginationIsLoading(),
        },
      },
      template: `
        <Pagination :count="7" :page="page" :onChange="clicked" :isLoading="isLoading" />
      `,
      data: function () {
        return {
          page: 1,
        }
      },
      methods: {
        clicked(page) {
          this.page = page;
        }
      }
    }}
  </Story>
</Preview>

### Loading state

<Preview>
  <Story name="Loading state" parameters={{ decorators: [withA11y] }} inline={true}>
    {{
      components: { Pagination },
      template: `
        <div >
          <Pagination :count="10" :page="4" :onChange="() => {}" :isLoading="true" />
        </div>
      `
    }}
  </Story>
</Preview>


# Props

<Props of={Pagination} />
