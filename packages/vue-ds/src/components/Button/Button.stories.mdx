import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks'
import { withKnobs } from '@storybook/addon-knobs'
import { withA11y } from '@storybook/addon-a11y'

import { knobButtonLoading, knobButtonSizes } from '@venice/core/storybook/knobs'

import Button from './Button.vue'

<Meta title="Components|Buttons" component={Button} />

# Button

Buttons allow users to make some actions. You can add a `href` attr to use it as `<a>` element.

## Variants

### Filled buttons

<Preview>
  <Story name="Filled" parameters={{ decorators: [withKnobs,withA11y] }} inline={true}>
    {{
      components: { Button },
      props: {
        isLoading: {
          default: knobButtonLoading(),
        },
      },
      template: `
        <div class="row-items">
          <Button :isLoading="isLoading">Default</Button>
          <Button :isLoading="isLoading" color="primary">Primary</Button>
          <Button :isLoading="isLoading" color="secondary">Secondary</Button>
          <Button :isLoading="isLoading" disabled>Disabled</Button>
        </div>
      `,
    }}
  </Story>
</Preview>

### Outlined buttons

<Preview>
  <Story name="Outlined" parameters={{ decorators: [withKnobs,withA11y] }} inline={true}>
    {{
      components: { Button },
      props: {
        isLoading: {
          default: knobButtonLoading(),
        },
      },
      template: `
        <div class="row-items">
          <Button :isLoading="isLoading" variant="outlined">Default</Button>
          <Button :isLoading="isLoading" color="primary" variant="outlined">
            Primary
          </Button>
          <Button :isLoading="isLoading" color="secondary" variant="outlined">
            Secondary
          </Button>
          <Button :isLoading="isLoading" disabled variant="outlined">
            Disabled
          </Button>
        </div>
      `,
    }}
  </Story>
</Preview>

### Text buttons

<Preview>
  <Story name="Text" parameters={{ decorators: [withKnobs,withA11y] }} inline={true}>
    {{
      components: { Button },
      props: {
        isLoading: {
          default: knobButtonLoading(),
        },
      },
      template: `
        <div class="row-items">
          <Button :isLoading="isLoading" variant="text">Default</Button>
          <Button :isLoading="isLoading" color="primary" variant="text">
            Primary
          </Button>
          <Button :isLoading="isLoading" color="secondary" variant="text">
            Secondary
          </Button>
          <Button :isLoading="isLoading" disabled variant="text">
            Disabled
          </Button>
        </div>
      `,
    }}
  </Story>
</Preview>

## Shapes

### Circle buttons

<Preview>
  <Story name="Circle" parameters={{ decorators: [withKnobs,withA11y] }} inline={true}>
    {{
      components: { Button },
      props: {
        isLoading: {
          default: knobButtonLoading(),
        },
        size: {
          default: knobButtonSizes(),
        },
      },
      template: `
        <div>
          <div class="row-items">
            <Button :isLoading="isLoading" shape="circle" :size="size">@</Button>
            <Button :isLoading="isLoading" color="primary" shape="circle" :size="size">@</Button>
            <Button :isLoading="isLoading" color="secondary" shape="circle" :size="size">@</Button>
            <Button :isLoading="isLoading" shape="circle" :size="size" disabled>@</Button>
          </div>
          <div class="row-items">
            <Button :isLoading="isLoading" variant="outlined" shape="circle" :size="size">@</Button>
            <Button :isLoading="isLoading" variant="outlined" color="primary" shape="circle" :size="size">@</Button>
            <Button :isLoading="isLoading" variant="outlined" color="secondary" shape="circle" :size="size">@</Button>
            <Button :isLoading="isLoading" variant="outlined" shape="circle" :size="size" disabled>@</Button>
          </div>
          <div class="row-items">
            <Button :isLoading="isLoading" variant="text" shape="circle" :size="size">@</Button>
            <Button :isLoading="isLoading" variant="text" color="primary" shape="circle" :size="size">@</Button>
            <Button :isLoading="isLoading" variant="text" color="secondary" shape="circle" :size="size">@</Button>
            <Button :isLoading="isLoading" variant="text" shape="circle" :size="size" disabled>@</Button>
          </div>
        </div>
      `,
    }}
  </Story>
</Preview>

## Sizes

### Medium buttons

<Preview>
  <Story name="Medium" parameters={{ decorators: [withKnobs,withA11y] }} inline={true}>
    {{
      components: { Button },
      props: {
        isLoading: {
          default: knobButtonLoading(),
        },
      },
      template: `
        <div>
          <div class="row-items">
            <Button :isLoading="isLoading" size="medium">Default</Button>
            <Button :isLoading="isLoading" color="primary" size="medium">Primary</Button>
            <Button :isLoading="isLoading" color="secondary" size="medium">Secondary</Button>
            <Button :isLoading="isLoading" size="medium" disabled>Disabled</Button>
          </div>
          <div class="row-items">
            <Button :isLoading="isLoading" variant="outlined" size="medium">Default</Button>
            <Button :isLoading="isLoading" variant="outlined" color="primary" size="medium">Primary</Button>
            <Button :isLoading="isLoading" variant="outlined" color="secondary" size="medium">Secondary</Button>
            <Button :isLoading="isLoading" variant="outlined" size="medium" disabled>Disabled</Button>
          </div>
          <div class="row-items">
            <Button :isLoading="isLoading" variant="text" size="medium">Default</Button>
            <Button :isLoading="isLoading" variant="text" color="primary" size="medium">Primary</Button>
            <Button :isLoading="isLoading" variant="text" color="secondary" size="medium">Secondary</Button>
            <Button :isLoading="isLoading" variant="text" size="medium" disabled>Disabled</Button>
          </div>
        </div>
      `,
    }}
  </Story>
</Preview>

### Small buttons

<Preview>
  <Story name="Small" parameters={{ decorators: [withKnobs,withA11y] }} inline={true}>
    {{
      components: { Button },
      props: {
        isLoading: {
          default: knobButtonLoading(),
        },
      },
      template: `
        <div>
          <div class="row-items">
            <Button :isLoading="isLoading" size="small">Default</Button>
            <Button :isLoading="isLoading" color="primary" size="small">Primary</Button>
            <Button :isLoading="isLoading" color="secondary" size="small">Secondary</Button>
            <Button :isLoading="isLoading" size="small" disabled>Disabled</Button>
          </div>
          <div class="row-items">
            <Button :isLoading="isLoading" variant="outlined" size="small">Default</Button>
            <Button :isLoading="isLoading" variant="outlined" color="primary" size="small">Primary</Button>
            <Button :isLoading="isLoading" variant="outlined" color="secondary" size="small">Secondary</Button>
            <Button :isLoading="isLoading" variant="outlined" size="small" disabled>Disabled</Button>
          </div>
          <div class="row-items">
            <Button :isLoading="isLoading" variant="text" size="small">Default</Button>
            <Button :isLoading="isLoading" variant="text" color="primary" size="small">Primary</Button>
            <Button :isLoading="isLoading" variant="text" color="secondary" size="small">Secondary</Button>
            <Button :isLoading="isLoading" variant="text" size="small" disabled>Disabled</Button>
          </div>
        </div>
      `,
    }}
  </Story>
</Preview>

## Icons

<Preview>
  <Story name="Icons" parameters={{ decorators: [withKnobs,withA11y] }} inline={true}>
    {{
      components: { Button },
      props: {
        isLoading: {
          default: knobButtonLoading(),
        },
      },
      template: `
        <div class="row-items" style="display: flex; align-items: center">
          <Button :isLoading="isLoading" variant="outlined">
            <template v-slot:startIcon>←</template>
            With icon
          </Button>
          <Button :isLoading="isLoading" variant="outlined">
            <template v-slot:endIcon>→</template>
            With icon
          </Button>
          <Button :isLoading="isLoading" size="small">
            <template v-slot:startIcon><span>←</span></template>
            With icon and tag
          </Button>
        </div>
      `,
    }}
  </Story>
</Preview>

## With loading

<Preview>
  <Story name="With loading" parameters={{ decorators: [withKnobs,withA11y] }} inline={true}>
    {{
      components: { Button },
      template: `
        <div class="row-items" style="display: flex; align-items: center">
          <Button isLoading="true" color="primary">
            Loading example
          </Button>
          <Button isLoading="true" size="small" color="secondary">
            Loading example
          </Button>
        </div>
      `,
    }}
  </Story>
</Preview>

# Props

<Props of={Button} />
