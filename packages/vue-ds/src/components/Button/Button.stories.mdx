import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks'
import { withA11y } from '@storybook/addon-a11y'

import Button from './Button.vue'

<Meta title="Components|Buttons" component={Button} />

# Button

You can add a `href` attr to use it as `<a>` element.

## Filled buttons

<Preview>
  <Story name="Filled" parameters={{ decorators: [withA11y] }} inline={true}>
    {{
      components: { Button },
      template: `
        <div class="row-items">
          <Button>Default</Button>
          <Button color="primary">Primary</Button>
          <Button color="secondary">Secondary</Button>
          <Button disabled>Disabled</Button>
        </div>
      `,
    }}
  </Story>
</Preview>

## Outlined buttons

<Preview>
  <Story name="Outlined" parameters={{ decorators: [withA11y] }} inline={true}>
    {{
      components: { Button },
      template: `
        <div class="row-items">
          <Button variant="outlined">Default</Button>
          <Button color="primary" variant="outlined">
            Primary
          </Button>
          <Button color="secondary" variant="outlined">
            Secondary
          </Button>
          <Button disabled variant="outlined">
            Disabled
          </Button>
        </div>
      `,
    }}
  </Story>
</Preview>

## Text buttons

<Preview>
  <Story name="Text" parameters={{ decorators: [withA11y] }} inline={true}>
    {{
      components: { Button },
      template: `
        <div class="row-items">
          <Button variant="text">Default</Button>
          <Button color="primary" variant="text">
            Primary
          </Button>
          <Button color="secondary" variant="text">
            Secondary
          </Button>
          <Button disabled variant="text">
            Disabled
          </Button>
        </div>
      `,
    }}
  </Story>
</Preview>

## Small buttons

<Preview>
  <Story name="Small" parameters={{ decorators: [withA11y] }} inline={true}>
    {{
      components: { Button },
      template: `
        <div>
          <div class="row-items">
            <Button size="small">Default</Button>
            <Button color="primary" size="small">Primary</Button>
            <Button color="secondary" size="small">Secondary</Button>
            <Button size="small" disabled>Disabled</Button>
          </div>
          <div class="row-items">
            <Button variant="outlined" size="small">Default</Button>
            <Button variant="outlined" color="primary" size="small">Primary</Button>
            <Button variant="outlined" color="secondary" size="small">Secondary</Button>
            <Button variant="outlined" size="small" disabled>Disabled</Button>
          </div>
          <div class="row-items">
            <Button variant="text" size="small">Default</Button>
            <Button variant="text" color="primary" size="small">Primary</Button>
            <Button variant="text" color="secondary" size="small">Secondary</Button>
            <Button variant="text" size="small" disabled>Disabled</Button>
          </div>
        </div>
      `,
    }}
  </Story>
</Preview>

## With Icons

<Preview>
  <Story name="With Icons" parameters={{ decorators: [withA11y] }} inline={true}>
    {{
      components: { Button },
      template: `
        <div class="row-items" style="display: flex">
          <Button variant="outlined">
            <template v-slot:startIcon>←</template>
            With Icon
          </Button>
          <Button variant="outlined">
            <template v-slot:endIcon>→</template>
            With Icon
          </Button>
        </div>
      `,
    }}
  </Story>
</Preview>

# Props

<Props of={Button} />
